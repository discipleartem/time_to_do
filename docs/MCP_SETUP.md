# MCP Настройка для Time to DO

## 📋 Обзор

MCP (Model Context Protocol) серверы настроены для персонального использования AI ассистентом при работе с проектом Time to DO.

## 🔧 Конфигурация

Файл конфигурации: `.windsurf/mcp.json`

### Активные серверы:

#### 1. **Filesystem Server**
- **Команда**: `npx @modelcontextprotocol/server-filesystem`
- **Доступ**: Полный доступ к файлам проекта `/home/tomas/WORK/task_to_do`
- **Использование**: Чтение/запись файлов, навигация по проекту

#### 2. **Git Server**
- **Команда**: `npx @modelcontextprotocol/server-git`
- **Доступ**: Git репозиторий проекта
- **Использование**: Просмотр истории, коммитов, веток

#### 3. **Brave Search Server**
- **Команда**: `npx @modelcontextprotocol/server-brave-search`
- **Требуется**: `BRAVE_API_KEY` в переменных окружения
- **Использование**: Веб-поиск для исследований

## 🚀 Установка и использование

### 1. Установка зависимостей
```bash
# Установка MCP серверов
npm install -g @modelcontextprotocol/server-filesystem
npm install -g @modelcontextprotocol/server-git
npm install -g @modelcontextprotocol/server-brave-search
```

### 2. Настройка API ключа
```bash
# Добавить в .env файл
BRAVE_API_KEY=your-brave-search-api-key
```

### 3. Проверка работы
```bash
# Проверить конфигурацию
cat .windsurf/mcp.json
```

## 📝 Возможности

### Файловая система:
- ✅ Чтение файлов проекта
- ✅ Создание и редактирование файлов
- ✅ Навигация по директориям
- ✅ Поиск файлов по шаблонам

### Git:
- ✅ Просмотр коммитов
- ✅ История изменений
- ✅ Информация о ветках
- ✅ Статус репозитория

### Веб-поиск:
- ✅ Поиск документации
- ✅ Исследование технологий
- ✅ Поиск решений проблем

## 🔒 Безопасность

- Доступ только к файлам проекта
- Нет доступа к системным файлам
- Git операции только для чтения
- Веб-поиск через API ключ

## 🛠️ Траблшутинг

### Проблема: MCP серверы не запускаются
```bash
# Проверить установку npm
npm list -g @modelcontextprotocol/server-filesystem

# Переустановить если нужно
npm install -g @modelcontextprotocol/server-filesystem
```

### Проблема: Нет доступа к файлам
```bash
# Проверить права доступа
ls -la /home/tomas/WORK/task_to_do

# Исправить если нужно
chmod -R 755 /home/tomas/WORK/task_to_do
```

### Проблема: Brave Search не работает
```bash
# Проверить API ключ
echo $BRAVE_API_KEY

# Добавить в .env если отсутствует
echo "BRAVE_API_KEY=your-key" >> .env
```

## 📚 Дополнительные ресурсы

- [MCP Documentation](https://modelcontextprotocol.io/)
- [Windsurf AI Assistant](https://docs.windsurf.ai/)
- [Time to DO Project](./development-plan-decomposed.md)

---

Эта конфигурация оптимизирована для эффективной работы AI ассистента с проектом Time to DO.
