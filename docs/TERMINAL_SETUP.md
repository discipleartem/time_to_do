# Настройка терминала в Windsurf для Time to DO

## 🚀 Проблема

В Windsurf терминал не активирует виртуальное окружение `.venv` автоматически, в отличие от VS Code.

## 🔧 Решение

### Конфигурация Windsurf

Создан файл `.windsurf/settings.json` с правильными настройками для автоматической активации виртуального окружения:

```json
{
  "python.defaultInterpreterPath": "./.venv/bin/python",
  "python.terminal.activateEnvironment": true,
  "python.terminal.activateEnvInCurrentTerminal": true,
  "terminal.integrated.env.linux": {
    "VIRTUAL_ENV": "${workspaceFolder}/.venv",
    "PYTHONPATH": "${workspaceFolder}",
    "PATH": "${workspaceFolder}/.venv/bin:${env:PATH}"
  }
}
```

## 📋 Инструкции

### Первичная настройка

1. **Перезапустите Windsurf** после добавления `.windsurf/settings.json`

2. **Откройте новый терминал** (Ctrl+Shift+`)

3. **Проверьте активацию**:
```bash
which python  # Должен указывать на .venv/bin/python
which pip     # Должен указывать на .venv/bin/pip
```

### Ежедневное использование

После правильной настройки терминал должен **автоматически** активировать виртуальное окружение при открытии.

Если автоматическая активация не работает:
```bash
# Ручная активация
source .venv/bin/activate
```

## 🛠️ Траблшутинг

### Проблема: Автоактивация не работает
1. **Перезапустите Windsurf** полностью
2. **Проверьте настройки** в `.windsurf/settings.json`
3. **Откройте новый терминал** (не используйте старый)
4. **Проверьте права доступа** к `.venv`

### Проблема: Python не найден
```bash
# Проверьте наличие окружения
ls -la .venv/

# Пересоздайте окружение
python3 -m venv .venv
```

### Проблема: Зависимости не установлены
```bash
# Установите зависимости
make install
# или
pip install -r requirements.txt
```

## 🎯 Лучшие практики

1. **Всегда перезапускайте Windsurf** после изменения настроек
2. **Используйте новый терминал** для проверки настроек
3. **Проверяйте версию Python** после активации
4. **Следите за переменными окружения**:
```bash
echo $VIRTUAL_ENV
echo $PATH | grep venv
```

## 📝 Ключевые настройки

| Настройка | Значение | Описание |
|-----------|----------|----------|
| `python.defaultInterpreterPath` | `./.venv/bin/python` | Путь к интерпретатору |
| `python.terminal.activateEnvironment` | `true` | Автоактивация |
| `python.terminal.activateEnvInCurrentTerminal` | `true` | Активация в текущем терминале |
| `VIRTUAL_ENV` | `${workspaceFolder}/.venv` | Переменная окружения |

---

**Важно:** Автоматическая активация виртуального окружения - это функция IDE, которая настраивается только через конфигурационные файлы, а не через скрипты.
