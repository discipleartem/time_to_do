# 🎨 Frontend архитектура Time to DO

## 📋 Обзор

Frontend Time to DO построен на современных технологиях с фокусом на производительность, адаптивность и пользовательский опыт. Архитектура следует принципам прогрессивного улучшения и минимальной сложности.

---

## 🛠️ Технологический стек

### Core технологии
- **Bootstrap 5.3** - UI фреймворк с адаптивной сеткой
- **Jinja2** - шаблонизация на стороне сервера
- **HTMX 1.9** - динамические обновления без SPA сложности
- **WebSocket** - real-time коммуникация

### JavaScript
- **Vanilla JS** - минимальные зависимости
- **Drag & Drop API** - нативная поддержка
- **Fetch API** - HTTP запросы
- **LocalStorage** - сохранение настроек

### CSS и стили
- **Bootstrap 5** - основная стилизация
- **Кастомные переменные** - темы и брендинг
- **CSS Grid/Flexbox** - современные лейауты
- **CSS анимации** - микро-взаимодействия

---

## 📁 Структура фронтенда

```
templates/
├── base.html                   # Основной шаблон
├── index.html                  # Главная страница
├── projects.html               # Управление проектами
├── kanban.html                 # Kanban доска
├── partials/                   # Переиспользуемые компоненты
│   └── task_card.html         # Карточка задачи
└── auth/                       # Аутентификация
    ├── login.html
    └── register.html

static/
├── css/
│   └── main.css               # Основные стили
├── js/
│   └── main.js                # JavaScript функционал
├── images/
│   ├── default-avatar.png     # Аватар по умолчанию
│   └── icons/                 # Иконки проекта
└── fonts/                     # Кастомные шрифты
```

---

## 🎨 Компонентная архитектура

### Базовый шаблон (base.html)
- **Навигация** - адаптивное меню с темой
- **Футер** - ссылки и информация
- **Модальные окна** - базовые стили
- **Уведомления** - toast система

### Страницы
- **index.html** - hero секция, статистика, быстрые действия
- **projects.html** - сетка проектов, модальное создание
- **kanban.html** - доска с drag & drop, фильтры

### Компоненты (partials/)
- **task_card.html** - карточка задачи с приоритетами
- **user_avatar.html** - аватар пользователя
- **status_badge.html** - бейдж статуса

---

## 🎯 Ключевые функции

### 1. Kanban доска
```javascript
// Drag & Drop функционал
function handleDragStart(e) {
    draggedElement = this;
    this.classList.add('dragging');
    e.dataTransfer.effectAllowed = 'move';
}

function handleDrop(e) {
    const taskId = draggedElement.dataset.taskId;
    const newStatus = this.dataset.status;
    moveTask(taskId, newStatus);
}
```

### 2. Time Tracker
```javascript
// Таймер для задач
function startTimer(taskId) {
    const response = await fetch(`/api/v1/time/entries/${taskId}/start`, {
        method: 'PUT'
    });
    updateTimerDisplay(taskId);
}
```

### 3. Управление темой
```javascript
// Переключение темной/светлой темы
function toggleTheme() {
    theme = theme === 'light' ? 'dark' : 'light';
    document.documentElement.setAttribute('data-bs-theme', theme);
    localStorage.setItem('theme', theme);
}
```

### 4. WebSocket коммуникация
```javascript
// Real-time обновления
websocket.onmessage = function(event) {
    const message = JSON.parse(event.data);
    handleWebSocketMessage(message);
};
```

---

## 🎨 Система стилей

### CSS переменные
```css
:root {
    --primary-gradient: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    --card-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    --hover-shadow: 0 8px 15px rgba(0, 0, 0, 0.2);
}

[data-bs-theme="dark"] {
    --card-shadow: 0 4px 6px rgba(0, 0, 0, 0.3);
}
```

### Адаптивный дизайн
```css
/* Mobile-first подход */
.kanban-board {
    padding: 1rem;
}

@media (max-width: 768px) {
    .kanban-column {
        margin: 0.25rem 0;
        min-height: 300px;
    }
}
```

### Анимации
```css
@keyframes fadeIn {
    from { opacity: 0; transform: translateY(10px); }
    to { opacity: 1; transform: translateY(0); }
}

.fade-in {
    animation: fadeIn 0.3s ease-in;
}
```

---

## 🔄 State management

### Клиентское состояние
- **LocalStorage** - тема, настройки пользователя
- **SessionStorage** - временные данные формы
- **JavaScript объекты** - состояние UI компонентов

### Синхронизация с сервером
- **WebSocket** - real-time обновления
- **Fetch API** - CRUD операции
- **HTMX** - динамические обновления

---

## 📱 Адаптивность

### Breakpoints
- **xs**: < 576px (телефоны)
- **sm**: 576px - 768px (большие телефоны)
- **md**: 768px - 992px (планшеты)
- **lg**: 992px - 1200px (маленькие десктопы)
- **xl**: ≥ 1200px (десктопы)

### Mobile-first подход
- Базовые стили для мобильных устройств
- Progressive enhancement для больших экранов
- Touch-friendly интерфейсы

---

## 🚀 Оптимизации

### Производительность
- **Lazy loading** изображений
- **Debounce** для поисковых запросов
- **Throttle** для частых обновлений
- **CSS containment** для оптимизации рендеринга

### Кэширование
- **Browser cache** для статических файлов
- **LocalStorage** для настроек
- **Service Worker** (в планах)

---

## 🔧 Интеграция с backend

### API эндпоинты
```javascript
// CRUD операции
const api = {
    projects: '/api/v1/projects',
    tasks: '/api/v1/tasks',
    timeEntries: '/api/v1/time/entries'
};

// WebSocket
const wsUrl = `${protocol}//${window.location.host}/ws`;
```

### Обработка ошибок
```javascript
try {
    const response = await fetch(url, options);
    if (!response.ok) throw new Error('API Error');
    return await response.json();
} catch (error) {
    showNotification({ type: 'error', text: error.message });
}
```

---

## 🧪 Тестирование фронтенда

### Unit тесты (в планах)
- JavaScript функции
- Компоненты
- Утилиты

### Integration тесты
- API интеграция
- WebSocket соединения
- Drag & Drop функционал

### E2E тесты (в планах)
- Playwright конфигурация
- Пользовательские сценарии
- Кросс-браузерное тестирование

---

## 🔒 Безопасность

### XSS защита
- **Jinja2 autoescaping** - автоматическое экранирование
- **Content Security Policy** - ограничения на контент
- **Sanitization** пользовательского ввода

### CSRF защита
- **FastAPI CSRF middleware**
- **SameSite cookies**
- **Token-based аутентификация**

---

## 📈 Метрики и аналитика

### Performance метрики
- **First Contentful Paint** (FCP)
- **Largest Contentful Paint** (LCP)
- **Cumulative Layout Shift** (CLS)
- **First Input Delay** (FID)

### User analytics (в планах)
- **Page views**
- **Feature usage**
- **Error tracking**
- **Performance monitoring**

---

## 🚀 Будущие улучшения

### Short term (1-2 месяца)
- [ ] PWA функционал (Service Worker)
- [ ] Офлайн режим
- [ ] Улучшенная доступность (a11y)
- [ ] Component-based архитектура

### Medium term (3-6 месяцев)
- [ ] Микрофронтенды
- [ ] Server-side rendering оптимизация
- [ ] Advanced caching стратегии
- [ ] Real-time collaboration

### Long term (6+ месяцев)
- [ ] WebAssembly для тяжелых вычислений
- [ ] WebRTC для P2P коммуникации
- [ ] AI-powered интерфейсы
- [ ] Voice navigation

---

## 🛠️ Разработка и отладка

### Инструменты разработчика
- **Browser DevTools** - отладка и профилирование
- **Lighthouse** - аудит производительности
- **Network tab** - анализ запросов
- **Console** - логирование и отладка

### Локальная разработка
```bash
# Запуск с hot reload
make dev

# Доступ к приложению
http://localhost:8000

# API документация
http://localhost:8000/api/v1/docs
```

---

## 📚 Лучшие практики

### Код организация
- **Component-based** подход
- **DRY принцип** - Don't Repeat Yourself
- **SOLID принципы** для JavaScript
- **Progressive Enhancement**

### Производительность
- **Minimal dependencies** - минимум зависимостей
- **Efficient DOM manipulation** - эффективная работа с DOM
- **Smart caching** - умное кэширование
- **Optimized images** - оптимизация изображений

### User Experience
- **Responsive design** - адаптивный дизайн
- **Fast interactions** - быстрые взаимодействия
- **Clear feedback** - понятная обратная связь
- **Accessibility** - доступность для всех

---

## 🎯 Заключение

Frontend архитектура Time to DO обеспечивает:
- **Масштабируемость** - легкое добавление новых функций
- **Производительность** - быстрая загрузка и взаимодействие
- **Поддерживаемость** - чистый и организованный код
- **User Experience** - интуитивный и приятный интерфейс

Архитектура готова к росту и развитию вместе с проектом!
