# Advanced Search - Полнофункциональный поиск

## 📋 Обзор

Система поиска Time to DO предоставляет мощные возможности полнотекстового поиска по всем сущностям проекта с использованием PostgreSQL TSVECTOR.

## 🚀 Возможности

### ✅ Полнотекстовый поиск
- **Релевантность** - ранжирование результатов по релевантности
- **Морфология** - поддержка русского языка (стемминг)
- **Оптимизация** - индексация на уровне БД для быстрого поиска

### ✅ Типы сущностей
- **Задачи** - поиск по названию и описанию
- **Проекты** - поиск по названию и описанию
- **Спринты** - поиск по названию, описанию и цели
- **Комментарии** - поиск по содержимому комментариев

### ✅ Фильтры и сортировка
- **Типы сущностей** - поиск по конкретным типам
- **Проекты** - ограничение поиска конкретными проектами
- **Публичные объекты** - включение/исключение публичных результатов
- **Пагинация** - постраничная навигация

### ✅ Сохраненные поиски
- **Персональные** - сохранение поисковых запросов
- **Публичные** - доступ к поискам других пользователей
- **Фильтры** - сохранение настроек фильтрации
- **Быстрый доступ** - мгновенное применение сохраненного поиска

## 🛠️ API Эндпоинты

### Поиск
```http
POST /api/v1/search/search
Content-Type: application/json
Authorization: Bearer <token>

{
  "query": "строка поиска",
  "entity_types": ["task", "project"],
  "project_ids": ["uuid-проекта"],
  "limit": 20,
  "offset": 0,
  "include_public": true
}
```

### GET Поиск
```http
GET /api/v1/search/search?q=задача&type=task&limit=20
Authorization: Bearer <token>
```

### Сохранение поиска
```http
POST /api/v1/search/saved-searches
Content-Type: application/json
Authorization: Bearer <token>

{
  "name": "Мои задачи",
  "query": "статус:в работе",
  "filters": {
    "entity_types": ["task"],
    "limit": 50
  },
  "is_public": false
}
```

### Получение сохраненных поисков
```http
GET /api/v1/search/saved-searches?public=true
Authorization: Bearer <token>
```

### Подсказки поиска
```http
GET /api/v1/search/suggestions?q=задач
Authorization: Bearer <token>
```

### Переиндексация
```http
POST /api/v1/search/reindex
Authorization: Bearer <token>
```

## 🎨 Frontend

### Страница поиска
- **URL**: `/search`
- **Real-time поиск** - автоматический поиск при вводе (debounce 500ms)
- **Фильтры** - выбор типов сущностей, лимитов, публичных объектов
- **Сохраненные поиски** - быстрая панель с сохраненными запросами
- **Пагинация** - удобная навигация по страницам результатов

### Навигация
- **Интеграция** - поиск добавлен в основную навигацию сайта
- **URL параметры** - сохранение состояния поиска в URL
- **История** - навигация по истории браузера работает корректно

### Результаты поиска
- **Релевантность** - процент релевантности для каждого результата
- **Типы** - цветовая индикация типа сущности
- **Метаданные** - дополнительная информация (статус, приоритет, проект и т.д.)
- **Переходы** - кликабельные результаты с переходом к сущности

## 🔄 Автоматическая индексация

### Декораторы `@auto_index`
```python
from app.services.search_decorators import auto_index

@auto_index(SearchableType.TASK)
async def create_task(self, task_data: TaskCreate, project_id: str, creator_id: str) -> Task:
    # Создание задачи
    task = Task(...)
    self.db.add(task)
    await self.db.commit()
    return task  # Автоматически проиндексирован
```

### Декораторы `@auto_index_delete`
```python
@auto_index_delete(SearchableType.TASK)
async def delete_task(self, task_id: str, user_id: str) -> bool:
    # Удаление задачи
    await self.db.delete(task)
    await self.db.commit()
    return True  # Автоматически удален из индекса
```

### Проиндексированные сущности
- **Задачи** - создание, обновление, удаление
- **Проекты** - создание, обновление, удаление
- **Комментарии** - создание, обновление, удаление
- **Спринты** - через переиндексацию

## 📊 Структура индекса

### SearchIndex
```sql
CREATE TABLE search_index (
    id UUID PRIMARY KEY,
    title VARCHAR(255) NOT NULL,
    content TEXT,
    search_vector TSVECTOR NOT NULL,  -- PostgreSQL полнотекстовый вектор
    entity_type searchabletype NOT NULL,
    entity_id UUID NOT NULL,
    project_id UUID,
    user_id UUID,
    is_public BOOLEAN DEFAULT FALSE,
    search_metadata TEXT,  -- JSON метаданные
    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
    updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);
```

### SavedSearch
```sql
CREATE TABLE saved_searches (
    id UUID PRIMARY KEY,
    name VARCHAR(255) NOT NULL,
    query TEXT NOT NULL,
    filters TEXT,  -- JSON фильтры
    user_id UUID NOT NULL,
    is_public BOOLEAN DEFAULT FALSE,
    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
    updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);
```

## 🎯 Использование

### Базовый поиск
```javascript
// API вызов
const response = await fetch('/api/v1/search/search', {
    method: 'POST',
    headers: {
        'Content-Type': 'application/json',
        'Authorization': `Bearer ${token}`
    },
    body: JSON.stringify({
        query: 'важная задача',
        limit: 20
    })
});

const results = await response.json();
console.log(results.results); // Массив результатов
```

### Расширенный поиск
```javascript
const searchQuery = {
    query: 'бэкенд разработка',
    entity_types: ['task', 'project'],  // Только задачи и проекты
    project_ids: ['project-uuid'],     // Конкретный проект
    include_public: false,               // Исключить публичные
    limit: 50,
    offset: 0
};
```

### Сохранение поиска
```javascript
const saveSearch = async () => {
    await fetch('/api/v1/search/saved-searches', {
        method: 'POST',
        headers: {
            'Content-Type': 'application/json',
            'Authorization': `Bearer ${token}`
        },
        body: JSON.stringify({
            name: 'Бэкенд задачи',
            query: currentQuery,
            filters: currentFilters,
            is_public: false
        })
    });
};
```

## 🔧 Оптимизация

### Индексы БД
- `search_vector` - GIN индекс для полнотекстового поиска
- `entity_type` - фильтрация по типам сущностей
- `entity_id` - быстрая проверка дубликатов
- `project_id` - фильтрация по проектам
- `user_id` - фильтрация по пользователям

### Кэширование
- **Результаты поиска** - кэширование популярных запросов
- **Сохраненные поиски** - кэширование списков поисков
- **Подсказки** - кэширование автодополнения

### Производительность
- **Пагинация** - ограничение количества результатов
- **Debounce** - задержка real-time поиска
- **Lazy loading** - подгрузка метаданных по требованию

## 🚨 Безопасность

### Проверки доступа
- **Авторизация** - все эндпоинты требуют токен
- **Доступ к сущностям** - проверка прав на объекты
- **Изоляция пользователей** - поиск только в доступных объектах

### Валидация
- **Длина запроса** - ограничение 500 символов
- **SQL инъекции** - защита через параметризованные запросы
- **XSS защита** - экранирование в frontend

## 📈 Мониторинг

### Метрики поиска
- **Количество запросов** - общая статистика использования
- **Популярные запросы** - анализ поисковых трендов
- **Производительность** - время выполнения запросов
- **Релевантность** - качество результатов поиска

### Логирование
- **Запросы** - логирование поисковых запросов
- **Ошибки** - логирование ошибок индексации
- **Производительность** - логирование медленных запросов

---

**Статус:** ✅ Полностью реализовано и готово к использованию
**Версия:** 1.0
**Обновлено:** 4 февраля 2026
