# External Sharing - Публичные ссылки

## 📋 Обзор

**External Sharing** - это система публичных ссылок для безопасного доступа к проектам и задачам Time to DO без регистрации. Пользователи могут создавать защищенные ссылки с различными уровнями доступа и ограничениями.

## 🚀 Возможности

### 🔗 Публичные ссылки
- **Уникальные токены** - 48-символьные безопасные токены
- **Типы объектов** - проекты, задачи, спринты
- **Красивые URL** - `/shared/{token}` для легкого доступа

### 🔐 Уровни доступа
- **View** - только просмотр контента
- **Comment** - просмотр и добавление комментариев

### ⚙️ Ограничения
- **Время действия** - автоматическое истечение ссылки
- **Лимит просмотров** - ограничение количества доступов
- **Парольная защита** - дополнительная безопасность
- **Активация/деактивация** - управление доступом

### 📊 Статистика
- **Счетчики просмотров** - отслеживание использования
- **Аналитика** - самые популярные ссылки
- **История доступа** - детальная статистика

## 🛠️ API Эндпоинты

### Управление ссылками
```http
POST   /api/v1/share-links/           # Создать ссылку
GET    /api/v1/share-links/           # Получить ссылки пользователя
GET    /api/v1/share-links/{id}       # Получить информацию о ссылке
PUT    /api/v1/share-links/{id}       # Обновить ссылку
DELETE /api/v1/share-links/{id}       # Удалить ссылку
GET    /api/v1/share-links/stats      # Статистика пользователя
```

### Публичный доступ
```http
POST   /api/v1/share-links/access      # Доступ по токену (с паролем)
GET    /api/v1/share-links/public/{token}  # Публичный доступ без аутентификации
```

## 📝 Примеры использования

### Создание публичной ссылки на проект
```python
import requests

# Создание ссылки
response = requests.post(
    "http://localhost:8000/api/v1/share-links/",
    headers={"Authorization": "Bearer <token>"},
    json={
        "shareable_type": "project",
        "shareable_id": "uuid-проекта",
        "permission": "view",
        "title": "Публичный доступ к проекту",
        "description": "Демонстрация проекта для клиентов",
        "expires_at": "2026-12-31T23:59:59Z",
        "max_views": 100
    }
)

share_link = response.json()
print(f"Публичная ссылка: {share_link['public_url']}")
```

### Доступ к контенту по ссылке
```python
# Получение контента без аутентификации
response = requests.get(
    f"http://localhost:8000/api/v1/share-links/public/{token}"
)

if response.status_code == 200:
    data = response.json()
    print(f"Контент: {data['content']}")
    print(f"Разрешены комментарии: {data['can_comment']}")
```

## 🎨 Frontend Интеграция

### Шаблон публичной страницы
- **Адаптивный дизайн** - работает на всех устройствах
- **Форма доступа** - ввод пароля при необходимости
- **Отображение контента** - проекты и задачи в красивом виде
- **Статистика** - информация о ссылке и просмотрах

### URL структура
```
/shared/{token}                    # Основная страница публичного доступа
/api/v1/share-links/public/{token} # JSON API для программного доступа
```

## 🔒 Безопасность

### Защита от злоупотреблений
- **Уникальные токены** - невозможно подобрать
- **Ограничения доступа** - время и количество просмотров
- **Проверка прав** - только владельцы могут создавать ссылки
- **Rate limiting** - защита от DDOS атак

### Проверка прав доступа
```python
# Проверка прав пользователя перед созданием ссылки
async def _check_access_permission(
    shareable_type: ShareableType,
    shareable_id: UUID,
    user_id: UUID
) -> None:
    if shareable_type == ShareableType.PROJECT:
        # Проверяем, что пользователь участник проекта
        member = await get_project_member(shareable_id, user_id)
        if not member:
            raise PermissionError("Нет доступа к проекту")
```

## 📊 Модели данных

### ShareLink
```python
class ShareLink(BaseModel):
    token: str                    # Уникальный токен доступа
    shareable_type: str           # Тип объекта (project/task/sprint)
    shareable_id: UUID            # ID объекта
    permission: str              # Уровень доступа (view/comment)
    password: str | None          # Пароль для доступа
    expires_at: datetime | None  # Время истечения
    max_views: int | None        # Максимальное количество просмотров
    current_views: int           # Текущее количество просмотров
    is_active: bool              # Активность ссылки
    created_by: UUID              # ID создателя
```

## 🧪 Тестирование

### Покрытие функциональности
- **Модель ShareLink** - создание, валидация, методы
- **ShareLinkService** - бизнес-логика и управление
- **API эндпоинты** - CRUD операции и безопасность
- **Генерация токенов** - уникальность и безопасность

### Быстрые тесты
```bash
# Запуск только быстрых тестов ShareLinks
pytest tests/test_share_links.py::TestShareLinkModel::test_share_link_creation -v
pytest tests/test_share_links.py::TestShareLinkService::test_generate_unique_token -v
```

## 🎯 Использование в реальных сценариях

### 📊 Демонстрация проектов клиентам
- Создайте временные ссылки для демонстрации
- Ограничьте время действия и количество просмотров
- Защитите конфиденциальные данные

### 👥 Совместная работа с внешними партнерами
- Предоставляйте доступ к конкретным задачам
- Устанавливайте права только на просмотр
- Отслеживайте активность использования

### 📱 Обмен информацией в мобильных приложениях
- Генерируйте QR-коды с публичными ссылками
- Обеспечьте быстрый доступ без регистрации
- Адаптивный дизайн для любых устройств

## 🔮 Будущие улучшения

### Планируемые возможности
- **QR-коды** - автоматическая генерация для мобильных устройств
- **Кастомизация** - настройка внешнего вида публичных страниц
- **Интеграции** - встраивание в другие платформы
- **Аналитика** - детальная статистика использования

### Масштабирование
- **Кэширование** - улучшение производительности для популярных ссылок
- **CDN интеграция** - ускорение загрузки статических ресурсов
- **Мониторинг** - отслеживание производительности и ошибок

---

## 📞 Поддержка

По вопросам использования External Sharing:
- 📧 Email: support@timetodo.com
- 📖 [Документация API](./API.md)
- 🐛 [GitHub Issues](https://github.com/yourusername/time_to_do/issues)

---

**External Sharing** - безопасный и удобный способ поделиться вашими проектами! 🔗
