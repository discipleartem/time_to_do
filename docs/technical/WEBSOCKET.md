# WebSocket Real-time Features

## 📋 Обзор

Time to DO включает полнофункциональную WebSocket инфраструктуру для real-time обновлений между пользователями. Это позволяет мгновенно синхронизировать изменения задач, комментариев и статусов проектов.

## 🏗️ Архитектура

### Core компоненты

#### `app/websocket/connection.py`
- **Connection** - управление индивидуальным WebSocket соединением
- Поддержка аутентифицированных и анонимных соединений
- Управление комнатами проектов
- Отправка JSON и текстовых сообщений

#### `app/websocket/manager.py`
- **ConnectionManager** - глобальный менеджер всех соединений
- Управление комнатами проектов
- Рассылка сообщений по проектам и пользователям
- Статистика и мониторинг соединений
- Автоматическая очистка неактивных соединений

#### `app/websocket/events.py`
- **EventType** - перечисление всех типов событий
- **WebSocketEvent** - базовая модель события
- Специализированные модели: TaskEvent, CommentEvent, ProjectEvent
- Фабрики функций для создания событий

#### `app/websocket/handlers.py`
- **WebSocketHandler** - обработчик WebSocket сообщений
- Аутентификация соединений через JWT токены
- Обработка ping/pong для поддержания соединения
- Управление комнатами проектов
- Интеграция с CRUD операциями

### Frontend клиент

#### `static/js/websocket.js`
- Автоматическое подключение и переподключение
- Аутентификация через JWT токены
- Управление комнатами проектов
- Обработка всех типов событий
- Индикаторы онлайн статуса

## 🚀 Использование

### Backend API

#### WebSocket эндпоинт
```
WS /api/v1/ws?token=<jwt_token>
```

#### Статистика соединений
```
GET /api/v1/ws/stats
```

### Frontend интеграция

#### Подключение
```javascript
// Автоматическое подключение при загрузке страницы
wsClient.connect();

// Присоединение к проекту
wsClient.joinProject('project-uuid');

// Обработка событий
wsClient.on('task', (data) => {
    console.log('Новое событие задачи:', data);
});
```

#### Отправка сообщений
```javascript
// Ping для поддержания соединения
wsClient.ping();

// Выход из проекта
wsClient.leaveProject('project-uuid');
```

## 📡 Типы событий

### Task события
- `task_created` - создание новой задачи
- `task_updated` - обновление задачи
- `task_moved` - перемещение задачи между статусами
- `task_deleted` - удаление задачи
- `task_assigned` - назначение исполнителя

### Comment события
- `comment_added` - добавление комментария
- `comment_updated` - обновление комментария
- `comment_deleted` - удаление комментария

### Project события
- `project_updated` - обновление проекта
- `project_member_added` - добавление участника
- `project_member_removed` - удаление участника

### User события
- `user_online` - пользователь онлайн
- `user_offline` - пользователь оффлайн

### System события
- `error` - ошибки и исключения
- `notification` - системные уведомления
- `ping` / `pong` - проверка соединения

## 🔧 Аутентификация

WebSocket поддерживает JWT аутентификацию через query параметр:

```javascript
// С токеном
const ws = new WebSocket('ws://localhost:8000/api/v1/ws?token=eyJhbGciOi...');

// Без токена (анонимное)
const ws = new WebSocket('ws://localhost:8000/api/v1/ws');
```

## 🏠 Комнаты проектов

Каждое соединение может присоединяться к комнатам проектов для получения обновлений только по нужным проектам:

```javascript
// Присоединение
wsClient.joinProject('project-uuid');

// Выход
wsClient.leaveProject('project-uuid');
```

## 📊 Статистика

API эндпоинт `/api/v1/ws/stats` возвращает:
- Количество активных соединений
- Количество аутентифицированных пользователей
- Активные комнаты проектов
- Общую статистику подключений

## 🧪 Тестирование

WebSocket функциональность покрыта 25 тестами в `tests/test_websocket_basic.py`:

- Connection тесты (7 тестов)
- ConnectionManager тесты (9 тестов)
- Events тесты (3 теста)
- WebSocketHandler тесты (5 тестов)
- API тесты (1 тест)

Запуск тестов:
```bash
pytest tests/test_websocket_basic.py -v
```

## 🔌 Интеграция с CRUD

WebSocket автоматически интегрирован в CRUD операции:

### Tasks API (`app/api/v1/tasks.py`)
- Создание задачи → `task_created` событие
- Обновление задачи → `task_updated` событие
- Удаление задачи → `task_deleted` событие
- Добавление комментария → `comment_added` событие

### Пример интеграции
```python
# После создания задачи
await handler.broadcast_task_event(
    EventType.TASK_CREATED,
    {
        "task_id": task.id,
        "project_id": str(task.project_id),
        "title": task.title,
        "status": task.status,
    },
    current_user.id
)
```

## 🛡️ Безопасность

- JWT валидация для аутентифицированных соединений
- Изоляция комнат проектов
- Валидация входящих сообщений
- Обработка ошибок без раскрытия информации
- Автоматическая очистка неактивных соединений

## 🚨 Обработка ошибок

### Клиентские ошибки
- Автоматическое переподключение при обрыве
- Экспоненциальная задержка между попытками
- Логирование ошибок в консоль

### Серверные ошибки
- Graceful degradation при ошибках
- Отправка error событий клиентам
- Мониторинг неактивных соединений

## 📈 Производительность

- Асинхронная обработка всех сообщений
- Минимальная задержка при рассылке
- Эффективное управление памятью
- Автоматическая очистка ресурсов

## 🔮 Будущие улучшения

1. **Notification System** - персистентные уведомления
2. **Browser Push** - push уведомления в браузере
3. **Presence Indicators** - индикаторы онлайн статуса пользователей
4. **Message History** - история сообщений для переподключения
5. **Rate Limiting** - ограничение частоты сообщений

---

**WebSocket инфраструктура обеспечивает полноценную real-time синхронизацию для всех пользователей Time to DO!** 🚀
