# 📢 Система уведомлений

**Дата создания:** 4 февраля 2026 года

## 📋 Обзор

Система уведомлений **Time to DO** предоставляет централизованную платформу для информирования пользователей о важных событиях в проекте. Система поддерживает real-time доставку через WebSocket и различные типы уведомлений.

## 🎯 Ключевые возможности

### 🔄 Real-time доставка
- **WebSocket интеграция** для мгновенной доставки уведомлений
- **Автоматическое обновление** счетчика непрочитанных уведомлений
- **Browser notifications** для нативных уведомлений браузера

### 📱 Frontend интерфейс
- **Dropdown в навигации** с последними уведомлениями
- **Отдельная страница** `/notifications` для управления всеми уведомлениями
- **Адаптивный дизайн** для мобильных устройств
- **Массовые операции** - отметить все как прочитанные, удалить прочитанные

### 🔧 Управление уведомлениями
- **Типизация уведомлений** - задачи, проекты, спринты, системные
- **Настройки пользователя** для управления типами уведомлений
- **Фильтры и поиск** по уведомлениям
- **История и статистика** уведомлений

## 🏗️ Архитектура

### 📊 Модель данных

```python
class Notification(BaseModel):
    user_id: UUID              # Получатель уведомления
    sender_id: UUID | None      # Отправитель (опционально)
    title: str                 # Заголовок уведомления
    message: str               # Текст уведомления
    notification_type: str      # Тип уведомления
    is_read: bool              # Прочитано ли
    read_at: datetime | None   # Время прочтения
    action_url: str | None    # URL для перехода
    project_id: UUID | None    # Связанный проект
    task_id: UUID | None       # Связанная задача
    sprint_id: UUID | None     # Связанный спринт
```

### 🔌 Сервисный слой

#### NotificationService
Основной сервис для управления уведомлениями:

```python
# Создание уведомления
notification = await service.create_notification(
    user_id=user.id,
    title="Новая задача",
    message="Вам назначена новая задача",
    notification_type=NotificationType.TASK_ASSIGNED
)

# Получение уведомлений
notifications = await service.get_user_notifications(
    user_id=user.id,
    unread_only=True,
    limit=20
)

# Отметка как прочитанное
await service.mark_notification_read(notification_id, user_id)
```

#### Удобные методы
```python
# Уведомление о назначении задачи
await service.notify_task_assigned(task_id, assignee_id, assigner_id)

# Уведомление о завершении спринта
notifications = await service.notify_sprint_completed(sprint_id, project_id)

# Уведомление всех участников проекта
await service.notify_project_member_added(project_id, member_id, inviter_id)
```

### 🌐 API эндпоинты

#### Основные эндпоинты
- `GET /api/v1/notifications/` - получить уведомления
- `GET /api/v1/notifications/stats` - статистика
- `GET /api/v1/notifications/unread-count` - количество непрочитанных
- `PATCH /api/v1/notifications/{id}/read` - отметить как прочитанное
- `POST /api/v1/notifications/bulk-action` - массовые операции
- `DELETE /api/v1/notifications/{id}` - удалить уведомление
- `GET/PUT /api/v1/notifications/preferences` - настройки

#### Примеры запросов

```bash
# Получить непрочитанные уведомления
curl -H "Authorization: Bearer <token>" \
     "http://localhost:8000/api/v1/notifications/?unread_only=true&limit=10"

# Отметить уведомление как прочитанное
curl -X PATCH \
     -H "Authorization: Bearer <token>" \
     -H "Content-Type: application/json" \
     -d '{"is_read": true}' \
     "http://localhost:8000/api/v1/notifications/<id>/read"

# Отметить все как прочитанные
curl -X PATCH \
     -H "Authorization: Bearer <token>" \
     "http://localhost:8000/api/v1/notifications/mark-all-read"
```

### 🔌 WebSocket интеграция

#### NotificationWebSocketIntegration
Класс для интеграции уведомлений с WebSocket:

```python
# Отправить уведомление пользователю
await notify_user_websocket(notification)

# Отправить уведомление всем участникам проекта
await notify_project_websocket(
    project_id=project.id,
    title="Проект обновлен",
    message="Проект был успешно обновлен"
)

# Отправить системное уведомление
await notify_system_websocket(
    title="Системное сообщение",
    message="Плановое обслуживание через 1 час"
)
```

## 📱 Frontend компоненты

### 📍 Dropdown уведомлений

Расположен в навигационной панели:

```html
<!-- Автоматически обновляется -->
<li class="nav-item dropdown">
    <a class="nav-link position-relative" href="#" data-bs-toggle="dropdown">
        <i class="bi bi-bell me-1"></i>
        <span class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger"
              id="notification-badge">0</span>
    </a>

    <!-- Список уведомлений -->
    <ul class="dropdown-menu dropdown-menu-end notification-dropdown">
        <!-- Уведомления загружаются через JavaScript -->
    </ul>
</li>
```

### 📄 Страница управления

Полнофункциональная страница `/notifications`:

- **Статистика** - общее количество, непрочитанные, за 24ч
- **Фильтры** - по типу, статусу, поиску
- **Массовые операции** - отметить все как прочитанные, очистить прочитанные
- **Пагинация** - для больших объемов уведомлений

## 🔔 Типы уведомлений

### 📋 Задачи
- `task_assigned` - Назначение задачи
- `task_completed` - Завершение задачи
- `task_overdue` - Просроченная задача
- `task_moved` - Перемещение задачи
- `task_comment_added` - Комментарий к задаче

### 🏢 Проекты
- `project_created` - Создание проекта
- `project_updated` - Обновление проекта
- `project_member_added` - Добавление участника
- `project_member_removed` - Удаление участника

### 🏃 Спринты
- `sprint_started` - Начало спринта
- `sprint_completed` - Завершение спринта
- `sprint_task_assigned` - Задача спринта

### ⚙️ Системные
- `system_announcement` - Системное объявление
- `welcome` - Приветственное уведомление
- `deadline_reminder` - Напоминание о дедлайне

## ⚙️ Настройки пользователя

Пользователи могут управлять типами уведомлений:

```python
class NotificationPreferences:
    # Уведомления о задачах
    task_assigned: bool = True
    task_completed: bool = True
    task_overdue: bool = True
    task_comment_added: bool = True

    # Уведомления о проектах
    project_created: bool = True
    project_member_added: bool = True
    project_updated: bool = True

    # Уведомления о спринтах
    sprint_started: bool = True
    sprint_completed: bool = True

    # Системные уведомления
    system_announcements: bool = True
    deadline_reminders: bool = True

    # Настройки доставки
    in_app_notifications: bool = True
    browser_notifications: bool = False
    email_notifications: bool = False
```

## 🧪 Тестирование

### 📊 Тестовое покрытие

- **Базовые тесты модели** - создание, обновление, удаление
- **Тесты сервиса** - CRUD операции, удобные методы
- **Тесты API** - все эндпоинты
- **Тесты WebSocket интеграции** - real-time доставка

### 🚀 Запуск тестов

```bash
# Все тесты уведомлений
pytest tests/ -k "notification" -v

# Базовые тесты
pytest tests/test_notifications_basic.py -v

# WebSocket интеграция
pytest tests/test_notification_websocket_integration.py -v
```

## 🔧 Конфигурация

### 📝 Переменные окружения

```bash
# Настройки уведомлений
NOTIFICATION_ENABLED=true
NOTIFICATION_BATCH_SIZE=100
NOTIFICATION_CLEANUP_DAYS=30

# WebSocket настройки
WEBSOCKET_ENABLED=true
WEBSOCKET_HEARTBEAT_INTERVAL=30
```

### 🐳 Docker конфигурация

```yaml
# docker-compose.yml
services:
  app:
    environment:
      - NOTIFICATION_ENABLED=true
      - WEBSOCKET_ENABLED=true
```

## 🚀 Best Practices

### 📌 Рекомендации

1. **Используйте типизированные методы** сервиса для создания уведомлений
2. **Проверяйте настройки пользователя** перед отправкой уведомлений
3. **Обрабатывайте ошибки** WebSocket доставки gracefully
4. **Используйте массовые операции** для оптимизации
5. **Очищайте старые уведомления** периодически

### ⚡ Оптимизация

```python
# Проверка настроек перед отправкой
if await service.should_send_notification(user_id, notification_type):
    await service.create_notification(notification_data)

# Массовая отправка
notifications = await service.create_notifications_batch([
    NotificationCreate(...),
    NotificationCreate(...),
])

# Очистка старых уведомлений
await service.cleanup_old_notifications(days=30)
```

## 🔮 Будущие улучшения

### 📋 Планируемые возможности

1. **Email уведомления** - интеграция с почтовыми сервисами
2. **Push уведомления** - для мобильных устройств
3. **Шаблоны уведомлений** - настраиваемые шаблоны
4. **Аналитика** - детальная статистика по уведомлениям
5. **API для внешних сервисов** - webhook интеграция

### 🎯 Roadmap

- **Q1 2026:** Email уведомления и шаблоны
- **Q2 2026:** Push уведомления и мобильное приложение
- **Q3 2026:** Расширенная аналитика и отчеты

---

## 📞 Поддержка

При возникновении вопросов по системе уведомлений:

1. Проверьте **документацию API** - `/docs` в приложении
2. Изучите **тесты** в `tests/test_notifications_*.py`
3. Проверьте **консоль браузера** для WebSocket ошибок
4. Обратитесь к **разработчикам** через GitHub Issues

**Система уведомлений Time to DO** создана для улучшения пользовательского опыта и обеспечения своевременной информированности о важных событиях в проекте. 🎉
