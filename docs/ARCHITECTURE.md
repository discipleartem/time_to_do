# Архитектура Time to DO

## 🏗️ Обзор архитектуры

Time to DO построен по принципам чистой архитектуры с четким разделением ответственности. Проект использует асинхронный подход для высокой производительности и масштабируемости.

---

## 📊 Архитектурные слои

### 1. Presentation Layer (API)
```
app/api/v1/
├── api.py           # Базовые API эндпоинты
├── auth.py          # Аутентификация и авторизация
├── users.py         # Управление пользователями
├── projects.py      # Управление проектами
├── tasks.py         # Управление задачами
└── github.py        # GitHub OAuth
```

**Ответственности:**
- Обработка HTTP запросов
- Валидация входных данных
- Аутентификация и авторизация
- Формирование HTTP ответов
- Обработка ошибок

### 2. Business Logic Layer (Services)
```
app/services/
├── auth_service.py       # Бизнес-логика аутентификации
├── user_service.py       # Бизнес-логика пользователей
├── project_service.py    # Бизнес-логика проектов
└── task_service.py        # Бизнес-логика задач
```

**Ответственности:**
- Реализация бизнес-правил
- Оркестрация данных
- Валидация бизнес-логики
- Интеграция с внешними сервисами

### 3. Data Access Layer (Models & Database)
```
app/models/
├── base.py           # Базовая модель
├── user.py           # Модель пользователя
├── project.py        # Модель проекта
├── task.py           # Модель задачи
├── sprint.py         # Модель спринта
└── time_entry.py     # Модель временных записей
```

**Ответственности:**
- Определение структуры данных
- SQLAlchemy ORM модели
- Валидация на уровне базы данных
- Связи между сущностями

### 4. Core Layer (Configuration & Security)
```
app/core/
├── config.py         # Конфигурация приложения
├── database.py       # Настройка PostgreSQL
├── redis.py          # Настройка Redis
└── security.py       # Безопасность и JWT
```

**Ответственности:**
- Управление конфигурацией
- Подключение к базам данных
- Безопасность и шифрование
- Управление сессиями

### 5. Authentication Layer
```
app/auth/
├── dependencies.py   # Зависимости аутентификации
└── service.py        # Сервис аутентификации
```

**Ответственности:**
- JWT токены
- GitHub OAuth
- Проверка прав доступа
- Управление пользователями

---

## 🔄 Поток данных

### Запрос на создание задачи
```
1. HTTP POST /api/v1/tasks/
   ↓
2. API Layer (tasks.py)
   - Валидация Pydantic схемы
   - Аутентификация пользователя
   ↓
3. Service Layer (task_service.py)
   - Бизнес-валидация
   - Проверка прав доступа
   - Расчет порядка задачи
   ↓
4. Data Layer (models/task.py)
   - Создание записи в БД
   - Возврат созданной задачи
   ↓
5. Service Layer
   - Отправка уведомлений (WebSocket)
   ↓
6. API Layer
   - Формирование ответа
   - Возврат HTTP 201
```

---

## 🗄️ База данных

### Структура
```sql
-- Пользователи
users (id, email, username, full_name, github_id, role, created_at)

-- Проекты
projects (id, name, description, owner_id, status, created_at)

-- Участники проектов
project_members (id, project_id, user_id, role, is_active, created_at)

-- Задачи
tasks (id, title, description, status, priority, project_id,
       creator_id, assignee_id, parent_task_id, order, created_at)

-- Комментарии
comments (id, content, task_id, author_id, is_edited, created_at)

-- Спринты
sprints (id, name, description, project_id, status,
         start_date, end_date, created_at)

-- Задачи спринтов
sprint_tasks (id, sprint_id, task_id, order, created_at)

-- Временные записи
time_entries (id, start_time, end_time, duration_minutes,
              user_id, task_id, is_active, created_at)

-- Уведомления
notifications (id, type, content, user_id, is_read, created_at)
```

### Индексы
```sql
-- Оптимизация запросов
CREATE INDEX idx_users_email ON users(email);
CREATE INDEX idx_projects_owner_id ON projects(owner_id);
CREATE INDEX idx_tasks_project_id_status ON tasks(project_id, status);
CREATE INDEX idx_tasks_assignee_id ON tasks(assignee_id);
CREATE INDEX idx_project_members_project_user ON project_members(project_id, user_id);
CREATE INDEX idx_time_entries_user_task ON time_entries(user_id, task_id);
```

---

## 🚀 WebSocket архитектура

### Компоненты
```
app/websocket/
├── manager.py       # Управление подключениями
├── connection.py    # Класс подключения
├── events.py        # Типы событий
└── handlers.py      # Обработчики событий
```

### Поток событий
```
1. Клиент подключается к WebSocket
2. Аутентификация по JWT токену
3. Подписка на комнаты (проекты)
4. Получение событий в реальном времени
5. Отправка событий другим участникам
```

### Типы событий
```python
# Изменение задачи
{
    "type": "task_updated",
    "data": {
        "task_id": "uuid",
        "project_id": "uuid",
        "changes": {"status": "in_progress"}
    }
}

# Новый комментарий
{
    "type": "comment_added",
    "data": {
        "task_id": "uuid",
        "comment": {...},
        "author": {...}
    }
}

# Пользователь онлайн
{
    "type": "user_online",
    "data": {
        "user_id": "uuid",
        "project_id": "uuid"
    }
}
```

---

## 🔐 Безопасность

### Аутентификация
- **JWT токены** с коротким сроком жизни
- **Refresh токены** для обновления
- **GitHub OAuth** для внешней аутентификации
- **Rate limiting** для защиты от брутфорса

### Авторизация
- **Ролевая модель**: Admin, Project Owner, Member, Viewer
- **Права доступа** на уровне проектов
- **Проверка прав** в каждом эндпоинте
- **Resource-based access control**

### Безопасность данных
- **Хеширование паролей** через bcrypt
- **Валидация входных данных** через Pydantic
- **SQL injection защита** через SQLAlchemy
- **CORS политика** для фронтенда

---

## ⚡ Производительность

### Оптимизации
- **Асинхронные операции** для I/O
- **Connection pooling** для базы данных
- **Redis кэширование** для частых запросов
- **Пагинация** для больших списков

### Мониторинг
- **Health checks** для всех сервисов
- **Performance метрики** запросов
- **Error tracking** и логирование
- **Database query analysis**

---

## 🔄 Интеграции

### GitHub OAuth
```python
# Поток аутентификации
1. Перенаправление на GitHub
2. Получение authorization code
3. Обмен на access token
4. Получение данных пользователя
5. Создание/обновление пользователя
6. Возврат JWT токенов
```

### Внешние сервисы
- **Email сервис** (SendGrid/SMTP)
- **File storage** (AWS S3/локальное)
- **Payment processor** (Stripe)
- **Analytics** (пользовательские метрики)

---

## 📱 Масштабирование

### Горизонтальное масштабирование
- **Stateless API** для балансировки нагрузки
- **Redis pub/sub** для WebSocket коммуникации
- **Database read replicas** для чтения
- **CDN** для статических файлов

### Вертикальное масштабирование
- **Async/await** для эффективного использования CPU
- **Connection pooling** для базы данных
- **Memory optimization** через кэширование
- **Background tasks** для тяжелых операций

---

## 🧪 Тестирование

### Стратегия тестирования
```
tests/
├── unit/           # Юнит-тесты сервисов
├── integration/    # Интеграционные тесты API
├── e2e/           # End-to-end тесты
└── fixtures/      # Тестовые данные
```

### Покрытие
- **Unit тесты**: бизнес-логика
- **Integration тесты**: API эндпоинты
- **E2E тесты**: пользовательские сценарии
- **Performance тесты**: нагрузочное тестирование

---

## 🚀 Deployment архитектура

### Production окружение
```
┌─────────────────┐    ┌─────────────────┐
│   Load Balancer │    │      CDN        │
└─────────┬───────┘    └─────────────────┘
          │
┌─────────▼───────┐    ┌─────────────────┐
│  FastAPI App    │    │   WebSocket     │
│  (Multiple      │    │   Server        │
│   instances)    │    └─────────────────┘
└─────────┬───────┘
          │
┌─────────▼───────┐    ┌─────────────────┐
│   PostgreSQL    │    │     Redis        │
│   (Primary)     │    │   (Cache + Pub)  │
└─────────────────┘    └─────────────────┘
```

### CI/CD Pipeline
```
1. Push to GitHub
2. Run tests (pytest)
3. Code quality (ruff, mypy)
4. Build Docker image
5. Deploy to staging
6. Run integration tests
7. Deploy to production
8. Health checks
```

---

## 📋 Принципы проектирования

### SOLID принципы
- **Single Responsibility**: Каждый класс имеет одну ответственность
- **Open/Closed**: Открыт для расширения, закрыт для изменения
- **Liskov Substitution**: Подтипы заменяемы базовыми типами
- **Interface Segregation**: Множество специфических интерфейсов
- **Dependency Inversion**: Зависимости от абстракций

### Clean Architecture
- **Independence of frameworks**: Независимость от фреймворков
- **Testability**: Легкость тестирования
- **Independence of UI**: Независимость от интерфейса
- **Independence of database**: Независимость от БД
- **Independence of external services**: Независимость от внешних сервисов

---

## 🔮 Будущие улучшения

### Микросервисы
- **User Service**: Управление пользователями
- **Project Service**: Управление проектами
- **Notification Service**: Уведомления
- **Analytics Service**: Аналитика и метрики

### Event-Driven Architecture
- **Event Bus**: Kafka/RabbitMQ
- **Event Sourcing**: История изменений
- **CQRS**: Разделение чтения/записи
- **Eventual Consistency**: Согласованность данных

### Advanced Features
- **Machine Learning**: Предсказание времени выполнения
- **Real-time Collaboration**: Совместное редактирование
- **Advanced Analytics**: Глубокая аналитика
- **Mobile API**: Специализированный API для мобильных приложений

---

Эта архитектура обеспечивает масштабируемость, поддерживаемость и надежность приложения Time to DO.
