# Python Version Configuration

## 🐍 Версия Python в проекте

**Основная версия:** Python 3.13+

### Конфигурационные файлы:

#### 1. pyproject.toml
```toml
[tool.poetry.dependencies]
python = "^3.13"

[tool.black]
target-version = ['py313']

[tool.ruff]
target-version = "py313"

[tool.mypy]
python_version = "3.13"
```

#### 2. .python-version
```
3.13
```

#### 3. README.md
```markdown
## 🐍 Требования
- **Python 3.13+** - основная версия разработки
```

### Особенности Python 3.13:

#### ✅ Новые возможности:
- Улучшенная производительность
- Новые синтаксические конструкции
- Улучшенная работа с типами
- Оптимизированный garbage collector

#### 🔄 Совместимость:
- Все зависимости проекта обновлены для Python 3.13
- Инструменты разработки (Black, Ruff, MyPy) настроены на py313
- Docker образы используют Python 3.13

#### 📦 Зависимости:
- FastAPI 0.128+ (полностью совместим с Python 3.13)
- SQLAlchemy 2.0.46+ (поддерживает Python 3.13)
- Pydantic 2.12+ (оптимизирован для Python 3.13)

### Правила разработки:

1. **Всегда использовать Python 3.13+** для разработки
2. **Type hints** обязательны (PEP 484, 585, 604)
3. **Современные типы**: `list[str]` вместо `List[str]`
4. **Match statements** где это уместно (Python 3.10+)
5. **Walrus operator** для оптимизации кода (Python 3.8+)

### Проверка версии:
```bash
python --version  # Должно быть 3.13.x
python -c "import sys; print(sys.version_info)"
```

### Обновление зависимостей:
```bash
make setup  # Пересоздаст окружение с Python 3.13
```

---
**Дата обновления:** 4 февраля 2026
**Версия конфигурации:** 1.0
