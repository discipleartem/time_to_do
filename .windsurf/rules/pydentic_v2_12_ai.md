# Pydantic v2.12 — Complete Ruleset for Agentic IDE (AI Agents)

Источник: официальная документация Pydantic v2.12
https://docs.pydantic.dev/2.12/

Документ предназначен **исключительно для агентных IDE и LLM‑агентов**, генерирующих,
рефакторящих и анализирующих Python‑код с использованием Pydantic v2.

---

## 0. Статус документа

- 📌 Версия: **Pydantic 2.12**
- 🎯 Цель: сделать Pydantic **формальным контрактом** между:
  - кодом
  - IDE
  - LLM
  - API
  - доменной логикой
- 🚫 Документ не является туториалом

---

## 1. Базовые аксиомы (Core Axioms)

1. **Все данные — Pydantic**
2. **Все схемы выводятся из типов**
3. **Валидация ≠ бизнес‑логика**
4. **Явность важнее удобства**
5. **IDE не догадывается — IDE читает модель**

---

## 2. Модели

### 2.1 BaseModel

Обязательные правила:
- Использовать `BaseModel` или `RootModel`
- Запрещено:
  - кастомный `__init__`
  - мутация входных данных

```python
from pydantic import BaseModel
```

IDE‑агент:
- Всегда предлагает модель вместо `dict`

---

### 2.2 RootModel

Используется, когда модель = одно значение

```python
from pydantic import RootModel

class UserId(RootModel[int]):
    pass
```

IDE:
- Должна предлагать RootModel для value‑objects

---

## 3. Конфигурация модели (ConfigDict)

```python
from pydantic import ConfigDict

model_config = ConfigDict(
    extra="forbid",
    frozen=True,
    validate_assignment=True,
    strict=True,
)
```

Обязательные правила:
- `extra="forbid"` — default
- `strict=True` — default
- `frozen=True` — default для domain‑моделей

IDE:
- Предупреждает о `extra="allow"`

---

## 4. Типовая система

### 4.1 Запрещённые типы
- `Any` (без комментария)
- `dict`
- `list` без параметров

### 4.2 Annotated

```python
from typing_extensions import Annotated
from pydantic import Field

Age = Annotated[int, Field(ge=0, le=150)]
```

IDE:
- Автогенерирует `Annotated`
- Не предлагает кастомный валидатор, если хватает Field

---

## 5. Валидаторы

### 5.1 field_validator

```python
@field_validator("email")
@classmethod
def validate_email(cls, v: str) -> str:
    ...
```

Правила:
- Без I/O
- Без сетевых запросов
- Без БД

---

### 5.2 model_validator

```python
@model_validator(mode="after")
def check_consistency(self):
    return self
```

Использовать только для:
- кросс‑полевых проверок

---

## 6. Функциональные валидаторы и сериализаторы

- Использовать из `pydantic.functional_validators`
- Предпочтительны для переиспользования

IDE:
- Предлагает функциональный вариант первым

---

## 7. Serialization / Deserialization

### 7.1 model_dump

Единственный допустимый API:

```python
model.model_dump()
model.model_dump(mode="json")
```

🚫 Запрещено:
- `dict()`
- `json()`

---

### 7.2 Aliases

```python
Field(alias="user_id")
```

IDE:
- Спрашивает про `populate_by_name`

---

## 8. JSON Schema

```python
model.model_json_schema()
```

Используется для:
- UI
- API
- LLM structured output

IDE:
- Никогда не генерирует схемы вручную

---

## 9. Errors

### 9.1 ValidationError

- Никогда не подавляется
- Всегда логируется

IDE:
- Показывает путь ошибки (`loc`)

---

## 10. TypeAdapter

Используется вместо временных моделей

```python
from pydantic import TypeAdapter
```

IDE:
- Предлагает TypeAdapter вместо ad‑hoc моделей

---

## 11. ORM / from_attributes

```python
ConfigDict(from_attributes=True)
```

Только для DTO

---

## 12. Performance

- Не создавать модели в циклах
- Кэшировать схемы
- Использовать `TypeAdapter` для hot‑path

---

## 13. Migration v1 → v2

IDE:
- Никогда не генерирует:
  - `@validator`
  - `@root_validator`
  - `Config`

---

## 14. Pydantic + LLM / Agents

### 14.1 Structured Output

- Любой вывод LLM → модель
- JSON без схемы запрещён

### 14.2 Retry Policy

- Retry при ValidationError
- max 3 попытки

---

## 15. Testing

IDE:
- Генерирует тесты из схем
- Проверяет:
  - invalid input
  - boundary values

---

## 16. Anti‑Patterns

❌ Any
❌ dict вместо модели
❌ логика в валидаторах
❌ JSON без схемы
❌ mutable domain‑модели

---

## 17. IDE Mandatory Checklist

- [ ] Все данные — Pydantic
- [ ] Нет Any
- [ ] strict=True
- [ ] extra=forbid
- [ ] model_dump
- [ ] ValidationError не подавляется
- [ ] LLM output валидируется

---

## 18. Итог

Pydantic v2 — это:
- типовая система
- контракт
- schema engine
- основа для агентных IDE

IDE, игнорирующая эти правила, **не является agentic**.
