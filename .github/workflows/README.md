# GitHub Actions Workflows

Эта директория содержит автоматические рабочие процессы для проекта Time to DO.

## 🔄 Workflows

### 1. Continuous Integration (`ci.yml`)
- Запускает тесты при каждом push и pull request
- Проверяет код стиль и типы
- Генерирует отчеты о покрытии кода

### 2. Documentation Sync (`docs-sync.yml`)
- Автоматически обновляет документацию при изменениях кода
- Генерирует API документацию из роутов
- Обновляет архитектурную документацию
- Обновляет README.md со статистикой проекта

### 3. Deployment (`deploy.yml`)
- Развертывает приложение на Render.com
- Запускается только при push в main ветку
- Включает health checks и rollback

## 🚀 Триггеры

### Автоматическая синхронизация документации
Документация обновляется при изменениях:
- `app/**/*.py` - любой файл в приложении
- `app/api/v1/*.py` - API роуты
- `app/models/*.py` - модели данных
- `app/schemas/*.py` - Pydantic схемы
- `pyproject.toml` - зависимости проекта

### CI/CD
- **Тесты**: каждый push и pull request
- **Деплой**: только push в `main` ветку
- **Документация**: изменения в кодовой базе

## 📋 Переменные окружения

Для работы workflows нужны следующие secrets:
- `RENDER_API_KEY` - API ключ для Render.com
- `RENDER_SERVICE_ID` - ID сервиса для деплоя

## 🔧 Отладка

### Локальный запуск скриптов документации
```bash
# Генерация API документации
python scripts/generate_api_docs.py

# Обновление архитектурной документации
python scripts/update_architecture_docs.py

# Обновление README.md
python scripts/update_readme.py
```

### Просмотр логов
```bash
# GitHub Actions логи
gh run list

# Детали конкретного запуска
gh run view <run-id>
```

## 📝 Кастомизация

### Добавление новых шагов
1. Создайте новый YAML файл в `.github/workflows/`
2. Определите триггеры (on)
3. Добавьте jobs и steps
4. Настройте переменные окружения

### Изменение триггеров документации
Отредактируйте `docs-sync.yml`:
```yaml
on:
  push:
    branches: [main, develop]
    paths:
      - 'app/**/*.py'
      - 'pyproject.toml'
```

---

Эти workflows обеспечивают автоматическую сборку, тестирование, развертывание и поддержку документации в актуальном состоянии.
